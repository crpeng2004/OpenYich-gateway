server:
  port: 9500
spring:
  application:
    name: openyich-gateway
  cloud:
    gateway:
      routes:
      - id: path_route
        uri: http://httpbin.org
        predicates:
        - Path=/get